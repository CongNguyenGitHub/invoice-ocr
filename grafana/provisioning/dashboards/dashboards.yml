apiVersion: 1

providers:
  - name: "Invoice OCR"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: true
    editable: false
    options:
      # Loaded from a separate mount so the provisioning/dashboards/ directory
      # (which ships the provider config) isn't shadowed by the dashboard files.
      path: /var/lib/grafana/dashboards
